package org.leadingsoft.golf.api.config;

//@Configuration
public class MailConfig {

  /*
   * @Bean public AmazonSimpleEmailService amazonSimpleEmailService(AWSCredentialsProvider
   * credentialsProvider) { return AmazonSimpleEmailServiceClientBuilder.standard().withCredentials(
   * credentialsProvider) // Replace US_EAST_1 with the AWS Region you're using for // Amazon SES.
   * .withRegion(Regions.US_EAST_1).build(); }
   * 
   * @Bean public MailSender mailSender(AmazonSimpleEmailService ses) { return new
   * SimpleEmailServiceMailSender(ses); }
   */

}
